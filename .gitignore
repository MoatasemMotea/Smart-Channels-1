# dependencies
/node_modules

# next.js
/.next
/out

# testing
/test-results
/playwright-report
/coverage

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# env — never commit real env files; .env.example is the template
.env
.env.local
.env.*.local

# local lead store (D-050) — never committed
.data/

# owner drop-box for raw product photography — never committed
/incoming-products/

# written by `next dev` on every start (Next 16 agent rules) — tool residue, not owner content (D-076)
AGENTS.md
CLAUDE.md
