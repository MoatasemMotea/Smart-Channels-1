# dependencies
/node_modules

# next.js
/.next
/out

# testing
/test-results
/playwright-report
/coverage

# misc
.DS_Store
*.tsbuildinfo
next-env.d.ts

# env — never commit real env files; .env.example is the template
.env
.env.local
.env.*.local

# local lead store (D-050) — never committed
.data/
